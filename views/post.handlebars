{{#each posts as |post| }}
  <div>
    <div>
      <h2> {{post.title}} </h2>
      <ul>
        {{#each post.comments as |comment|}}
          <li>{{comment.text}}</li>
        {{/each}}
      </ul>
    </div>
  </div>
{{/each}}